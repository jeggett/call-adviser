import {
  selectNewCallPage,
} from '../selectors';
import { fromJS } from 'immutable';
import expect from 'expect';

const selector = selectNewCallPage();

describe('selectNewCallPage', () => {

});
