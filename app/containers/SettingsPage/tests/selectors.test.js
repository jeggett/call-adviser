import {
  selectSettingsPage,
} from '../selectors';
import { fromJS } from 'immutable';
import expect from 'expect';

const selector = selectSettingsPage();

describe('selectSettingsPage', () => {

});
