import {
  selectDialogTreesPage,
} from '../selectors';
import { fromJS } from 'immutable';
import expect from 'expect';

const selector = selectDialogTreesPage();

describe('selectDialogTreesPage', () => {

});
